# KeePassXC preferences

`keepassxc.ini` is intentionally not managed by Home Manager. KeePassXC can
store a private KeeShare key in this file, so it must remain local and outside
Git. The installer preserves an existing copy during activation.
